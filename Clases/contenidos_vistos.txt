====================================================
KLARITY — MAPEO COMPLETO DE CONTENIDOS VISTOS EN CLASE
====================================================
Todos los temas extraídos de "Contenido clases.docx"
mapeados a archivos y líneas exactas del proyecto.

ESTADO: ✅ Implementado  |  ⚠️ Parcial  |  ❌ Falta

====================================================
BLOQUE 1 — ANDROID 101 (recursos / estructura)
====================================================

✅ Android Manifest
   → app/src/main/AndroidManifest.xml
   → Declara MainActivity, @AndroidEntryPoint (Hilt), permisos

✅ Drawables / res/drawable (independientes de densidad)
   → app/src/main/res/drawable/ic_klarity_logo.xml  (vector drawable)
   → Usado en KlarityLogoIcon vía Icons.Default.Bolt

✅ Mipmap (iconos de launcher en múltiples densidades)
   → app/src/main/res/mipmap-*/ic_launcher*.png

✅ Layouts y animaciones / res/layout  ← EN COMPOSE no se usa XML de layout
   → Equivalente Compose: cada @Composable define el árbol visual
   → Animaciones: SplashScreen.kt:22-38 (Animatable, spring, tween)

✅ Colores / res/values/colors.xml
   → ui/theme/Color.kt — todos los tokens de color (Blue500, Cyan500, etc.)

✅ Values / res/values/strings.xml
   → app/src/main/res/values/strings.xml + values-en/strings.xml  (i18n ES/EN)

✅ El Archivo R
   → Generado por build; usado extensivamente con stringResource(R.string.xxx),
     Icons, etc. en todas las pantallas.

====================================================
BLOQUE 2 — KOTLIN
====================================================

✅ val (propiedades inmutables)
   → Models.kt:6-37  — todos los campos de data class son val
   → MockData.kt — currentUser, purchases, etc. son val

✅ Tipos de datos / inferencia de tipos
   → Models.kt:6-37  (Int, String, Double, List<Product>, LocalDate, etc.)
   → Inferencia: val selected = currentRoute == item.route  (Components.kt:72)

✅ Declaración de funciones
   → Toda función @Composable; ej. fun NavGraph(navController: NavHostController)
     NavGraph.kt:22

✅ Funciones de orden superior
   → Components.kt:111 — actions: @Composable RowScope.() -> Unit = {}
   → NavGraph.kt:66 — onNavigateToPurchaseDetail: (Int) -> Unit

✅ Expresiones lambda
   → NavGraph.kt:30 — onNavigateToLogin = { navController.navigate(...) }
   → Components.kt:71 — bottomNavItems.forEach { item -> ... }

✅ Expresiones lambda como parámetro
   → KlarityButton: onClick: () -> Unit  (Components.kt:487)
   → items(MockData.recentPurchases) { purchase -> PurchaseCard(...) }  HomeScreen.kt:170

✅ Manejo de valores nulos / Safe calls
   → NavGraph.kt:137 — backStack.arguments?.getInt("purchaseId") ?: 1
   → Models.kt:12,22,31 — avatarUrl: String? = null

✅ Elvis operator "?:"
   → NavGraph.kt:137,158,173,174

✅ Uso de "let"
   ⚠️ No explícito actualmente — se puede agregar en validación de LoginScreen

✅ Funciones de extensión
   → DateTimeFormatter.ofPattern(...).format(purchase.date)  HomeScreen.kt:34-35
   ⚠️ No hay extensiones propias declaradas explícitamente — fácil de agregar

✅ Scope functions (apply, let, run, also, with)
   ⚠️ No explícitas — usado implícitamente en lambdas de Compose

✅ Definición de una clase
   → SuperAhorroApp.kt — class SuperAhorroApp : Application()
   → MainActivity.kt:16 — class MainActivity : ComponentActivity()

✅ Clase: Constructores
   → Models.kt — data class con constructor primario (val id: Int, ...)

✅ Clase: Setters y Getters
   → Data classes generan getters automáticamente (component functions)
   → var email by remember { mutableStateOf("") }  LoginScreen.kt:33 — setter/getter implícito

✅ Data Classes
   → Models.kt:6,15,25,34 — User, Purchase, Product, StatSummary

✅ Control Flow (if, when, for, while)
   → if: Components.kt:123,306,327,468 y muchos más
   → when: HomeScreen.kt:74 — when (route) { "history" -> ... }
   → for (forEach): Components.kt:71

✅ Corutinas
   → SplashScreen.kt:25 — LaunchedEffect(Unit) { delay(1200L); onNavigateToLogin() }
   → HomeScreen.kt:32 — val scope = rememberCoroutineScope()
   → HomeScreen.kt:59 — kotlinx.coroutines.runBlocking { drawerState.open() }

✅ Colecciones mutables en Kotlin
   → MockData.kt — mutableListOf<Purchase>(...) o listOf(...)
   → Components.kt:35 — val bottomNavItems = listOf(...)

====================================================
BLOQUE 3 — ACTIVITIES Y LAYOUTS
====================================================

✅ Activities / Ciclo de vida de una Activity
   → MainActivity.kt:16 — class MainActivity : ComponentActivity()
   → override fun onCreate(savedInstanceState: Bundle?)  MainActivity.kt:17

✅ Declarar una Activity
   → AndroidManifest.xml — <activity android:name=".MainActivity" ...>
   → @AndroidEntryPoint  MainActivity.kt:15

✅ Layout (en Compose = árbol de Composables)
   → Equivalente XML: Column, Row, Box, Scaffold — ej. HomeScreen.kt:54-185

✅ Componente gráfico: TextView → Text en Compose
   → Todos los Text(...) en cada pantalla

✅ ViewGroup: LinearLayout → Column / Row en Compose
   → Column: LoginScreen.kt:43; Row: Components.kt:196

✅ ViewGroup: RelativeLayout → Box + Alignment en Compose
   → Box(contentAlignment = Alignment.Center)  SplashScreen.kt:41

✅ ViewGroup: FrameLayout → Box en Compose
   → Components.kt:146 (KlarityLogoIcon), 209

✅ ViewGroup: ScrollView → verticalScroll / LazyColumn
   → LoginScreen.kt:46 — rememberScrollState() + verticalScroll
   → HomeScreen.kt:90 — LazyColumn

✅ ViewGroup: GridLayout → LazyVerticalGrid (en StatsScreen)
   ⚠️ No se usa LazyVerticalGrid explícitamente; Row con weight simula grid

✅ Coordinator Layout → Scaffold en Compose
   → HomeScreen.kt:54 — Scaffold(topBar, bottomBar, floatingActionButton)

✅ Atributos de una View (modifier, color, padding, size, weight)
   → Modifier.fillMaxWidth(), .padding(), .size(), .weight(1f) — en todas las pantallas

✅ Listeners y findViewByIds → onClick / eventos en Compose
   → Card(onClick = { ... })  Components.kt:182
   → IconButton(onClick = { ... })  Components.kt:337

✅ ViewBinding → En Compose NO existe ViewBinding (se reemplaza completamente)
   → setContent { ... }  MainActivity.kt:20 — equivalente moderno

✅ Intent
   → NavGraph.kt — navController.navigate(Routes.X.route) — equivalente a Intent explícito

✅ Tipos de Intent: Explícitos → navController.navigate(route)
   → NavGraph.kt:41,46,54,65-77,etc.

✅ Tipos de Intent: Implícitos
   ⚠️ No hay Intent implícito actualmente — se puede agregar para compartir ticket/foto

✅ Formas de iniciar una Activity / popUpTo
   → NavGraph.kt:31-32 — popUpTo(Routes.Splash.route) { inclusive = true }
   → NavGraph.kt:42-43 — popUpTo(Routes.Login.route) { inclusive = true }

✅ Parcelables / Bundles — NavArguments en Compose Navigation
   → NavGraph.kt:121 — arguments = listOf(navArgument("purchaseId") { type = NavType.IntType })
   → NavGraph.kt:123 — backStack.arguments?.getInt("purchaseId")

✅ Intent-filters
   → AndroidManifest.xml — <intent-filter> con MAIN + LAUNCHER para MainActivity

====================================================
BLOQUE 4 — JETPACK COMPOSE
====================================================

✅ Importar dependencias — build.gradle.kts
   → compose BOM, material3, navigation-compose, hilt-navigation-compose

✅ Declarar función como @Composable
   → TODAS las funciones de UI: @Composable fun HomeScreen(...)

✅ Gestión del estado de la UI
   → var email by remember { mutableStateOf("") }  LoginScreen.kt:33-36

✅ Configuración del proyecto
   → app/build.gradle.kts — buildFeatures { compose = true }, composeOptions

✅ Modifier
   → Usado en absolutamente todos los @Composable

✅ Contenedores: Column
   → LoginScreen.kt:43, SplashScreen.kt:47, Components.kt:375

✅ Contenedores: Row
   → HomeScreen.kt:134, Components.kt:196,288

✅ Contenedores: Box
   → SplashScreen.kt:41, Components.kt:146,209,379,425

✅ Contenedores: LazyColumn y LazyRow
   → HomeScreen.kt:90 — LazyColumn con items()

✅ Scaffold
   → HomeScreen.kt:54 — Scaffold(topBar, bottomBar, floatingActionButton, content)

✅ Modifier para contenedores (fillMaxSize, fillMaxWidth, padding, weight, etc.)
   → Extensivamente en todas las pantallas

✅ Componentes: Text
   → En todas las pantallas

✅ Componentes: Button
   → KlarityButton (Components.kt:486) — envuelve Button de Material3

✅ Componentes: Image
   → ⚠️ No hay Image explícita (logo usa Icon+Box); fácil agregar en ProfileScreen

✅ Componentes: TextField y controles de selección
   → OutlinedTextField: LoginScreen.kt:93,111
   → Checkbox/RadioButton: ⚠️ No implementados aún

✅ Componentes: Card
   → PurchaseCard (Components.kt:181), StatCard (Components.kt:369)
   → LoginScreen.kt:72 — Card del formulario

✅ Componentes: Checkbox
   ❌ No implementado — agregar en SettingsScreen o ProductFormScreen

✅ Componentes: HorizontalDivider
   → HomeScreen.kt:219,235

✅ Componentes: RadioButton
   ❌ No implementado — agregar en SettingsScreen (modo de ordenamiento)

✅ Componentes: Icon
   → Extensivamente: Icons.Default.*, Icons.AutoMirrored.*

✅ Componentes: Slider
   ❌ No implementado — agregar en StatsScreen (filtro de rango de fechas)

✅ Manejando el estado en Compose / Recomposición
   → LoginScreen.kt:33-36 — mutableStateOf + remember

✅ Remember
   → SplashScreen.kt:22-23 — remember { Animatable(0f) }
   → LoginScreen.kt:33-36 — remember { mutableStateOf("") }

✅ mutableStateOf
   → LoginScreen.kt:33-36

✅ Side Effects: LaunchedEffect
   → SplashScreen.kt:25 — LaunchedEffect(Unit) { ... delay ... onNavigateToLogin() }

✅ Navigation Compose / Implementar Navigation Compose
   → NavGraph.kt:22-182 — NavHost, composable, navArgument, NavType

✅ Preview
   ⚠️ No hay @Preview anotations actualmente — agregar a 2-3 composables clave

====================================================
BLOQUE 5 — VIEWMODEL Y CORUTINAS
====================================================

✅ Clean Architecture en Android
   → Estructura de paquetes: model/, ui/screens/, ui/components/, ui/navigation/, ui/theme/

✅ ViewModel — el "State holder" de la UI
   ❌ No hay ViewModels implementados aún (pantallas usan MockData directamente)
   → NECESARIO AGREGAR para la presentación

✅ Flujo de estados (StateFlow / UiState)
   ❌ Depende de ViewModel — NECESARIO AGREGAR

✅ Corutinas → Concurrencia
   → SplashScreen.kt:25 — LaunchedEffect + delay
   → HomeScreen.kt:59 — runBlocking { drawerState.open() }

✅ Lanzar la corutina (launch)
   → HomeScreen.kt:32 — rememberCoroutineScope()
   ⚠️ scope.launch{} no se usa explícitamente todavía

✅ CoroutineScope / Tipos de Scope
   → viewModelScope (necesita ViewModel)
   → rememberCoroutineScope()  HomeScreen.kt:32
   → LaunchedEffect scope  SplashScreen.kt:25

====================================================
RESUMEN DE BRECHAS A CUBRIR
====================================================

CRITICO (necesario para la presentación):
  ❌ ViewModel con StateFlow  → al menos 1 ViewModel (HomeViewModel o LoginViewModel)
  ❌ scope.launch{}            → lanzar corutina desde ViewModel
  ❌ Checkbox                  → agregar en SettingsScreen o ProductFormScreen
  ❌ RadioButton               → agregar en SettingsScreen
  ❌ Slider                    → agregar en StatsScreen o SettingsScreen
  ❌ @Preview                  → agregar en 2-3 composables

DESEABLE:
  ⚠️ Implicit Intent           → compartir ticket por WhatsApp/email
  ⚠️ scope.launch en lugar de runBlocking en HomeScreen
  ⚠️ Funciones de extensión propias
  ⚠️ uso explícito de "let"

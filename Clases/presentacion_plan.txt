====================================================
KLARITY — PLAN DE PRESENTACIÓN (≤10 min)
====================================================
Objetivo: mostrar TODO el contenido de las clases
usando fragmentos del código real del proyecto.
Formato: 1 fragmento de código por slide + flechitas
a líneas específicas indicando qué concepto se aplica.

TIEMPO ESTIMADO: ~8 min de slides + 2 min Q&A/demo

====================================================
SLIDE 1 — ESTRUCTURA DEL PROYECTO ANDROID (1 min)
====================================================
Fragmento: AndroidManifest.xml (extracto) + árbol de paquetes

Conceptos cubiertos:
  → <manifest> + <application>   = Android Manifest
  → <activity android:name>      = Declarar una Activity
  → <intent-filter MAIN/LAUNCHER>= Intent-filter
  → Árbol model/ ui/ navigation/ = Clean Architecture en Android
  → @AndroidEntryPoint (MainActivity.kt:15) = Hilt DI

Mostrar: AndroidManifest.xml + captura del árbol de carpetas en Android Studio

====================================================
SLIDE 2 — KOTLIN: DATA CLASSES, val, NULOS (1 min)
====================================================
Fragmento: Models.kt (completo, 37 líneas)

Conceptos cubiertos:
  → línea 6,15,25,34   = data class (User, Purchase, Product, StatSummary)
  → línea 7,8,9...     = val + tipos de datos (Int, String, Double)
  → línea 12,22,31     = String? = null → tipos nullable
  → línea 6-13         = Constructores primarios / Setters y Getters automáticos
  → inferencia de tipos: val products: List<Product> = emptyList()

====================================================
SLIDE 3 — KOTLIN: LAMBDAS, FUNCIONES DE ORDEN SUPERIOR, NULLS (1 min)
====================================================
Fragmento: NavGraph.kt líneas 22-50

Conceptos cubiertos:
  → línea 22           = fun NavGraph(...) → declaración de función
  → línea 30           = onNavigateToLogin = { navController.navigate(...) }
                         → expresión lambda como parámetro
  → línea 66           = onNavigateToPurchaseDetail: (Int) -> Unit
                         → función de orden superior
  → línea 137          = backStack.arguments?.getInt("purchaseId") ?: 1
                         → safe call ?. + Elvis operator ?:
  → líneas 121,135,169 = navArgument("purchaseId") { type = NavType.IntType }
                         → equivalente a Bundle / Parcelable
  → línea 31-32        = popUpTo { inclusive = true } → formas de iniciar Activity

====================================================
SLIDE 4 — ACTIVITIES, CICLO DE VIDA Y COMPOSE (1 min)
====================================================
Fragmento: MainActivity.kt (completo, 29 líneas)

Conceptos cubiertos:
  → línea 16           = class MainActivity : ComponentActivity()
                         → Activities / herencia
  → línea 17           = override fun onCreate(savedInstanceState: Bundle?)
                         → Ciclo de vida de una Activity
  → línea 20           = setContent { } → reemplaza setContentView / ViewBinding
  → línea 19           = enableEdgeToEdge() → atributos de View
  → línea 21-24        = SuperAhorroTheme { NavGraph() }
                         → Configuración del proyecto Compose
  → línea 15           = @AndroidEntryPoint → Hilt

====================================================
SLIDE 5 — JETPACK COMPOSE: STATE, REMEMBER, SIDE EFFECTS (1.5 min)
====================================================
Fragmento: SplashScreen.kt (completo, 73 líneas)

Conceptos cubiertos:
  → línea 21           = @Composable fun SplashScreen(...)
                         → Declarar función como @Composable
  → línea 22-23        = remember { Animatable(0f) }
                         → remember + mutableStateOf
  → línea 25           = LaunchedEffect(Unit) { ... }
                         → Side Effect → LaunchedEffect
  → línea 26-32        = scale.animateTo(spring(...))
                         → animaciones (equivalente /res/anim)
  → línea 37           = delay(1200L) → Corutinas / concurrencia
  → línea 41-72        = Box + Column + Modifier.fillMaxSize()
                         → Box (FrameLayout), Column (LinearLayout), Modifier
  → líneas 59,65       = Text(...) → componente Text (TextView)

====================================================
SLIDE 6 — COMPOSE: SCAFFOLD, TOPBAR, BOTTOMBAR, FAB, LAZY (1.5 min)
====================================================
Fragmento: HomeScreen.kt líneas 63-100

Conceptos cubiertos:
  → línea 63           = Scaffold(topBar, bottomBar, floatingActionButton)
                         → Coordinator Layout equivalente
  → línea 64           = TopAppBar { ... } → componente TopBar
  → línea 79           = AppBottomNavigationBar → NavigationBar
  → línea 89           = ExtendedFloatingActionButton → FAB
  → línea 97           = LazyColumn { items(lista) { ... } }
                         → LazyColumn (RecyclerView / ScrollView)
  → línea 82           = when (route) { "history" -> ... }
                         → Control Flow: when
  → línea 66           = scope.launch { drawerState.open() }
                         → Lanzar corutina con launch / CoroutineScope

====================================================
SLIDE 7 — COMPOSE: TEXTFIELD, CARD, BUTTON, ESTADO LOCAL (1 min)
====================================================
Fragmento: LoginScreen.kt líneas 33-36 + 72-155

Conceptos cubiertos:
  → línea 33-36        = var email by remember { mutableStateOf("") }
                         → mutableStateOf + remember + Recomposición
  → línea 93           = OutlinedTextField(...) → TextField y controles
  → línea 101          = KeyboardOptions(keyboardType = KeyboardType.Email)
  → línea 102          = isError = emailError → validación de estado
  → línea 119-126      = trailingIcon { IconButton } → Icon + listener
  → línea 128-129      = PasswordVisualTransformation() → control de selección
  → línea 148          = KlarityButton(onClick = { if ... }) → Button
  → línea 72-83        = Card + CardDefaults → componente Card

====================================================
SLIDE 8 — VIEWMODEL, STATEFLOW Y CORUTINAS (1.5 min)
====================================================
Fragmento: HomeViewModel.kt (completo, ~55 líneas)

Conceptos cubiertos:
  → línea 20-27        = data class HomeUiState(...) → Flujo de estados (UI State)
  → línea 30           = @HiltViewModel + class HomeViewModel : ViewModel()
                         → ViewModel: el "State holder" de la UI
  → línea 32           = MutableStateFlow(HomeUiState())
                         → StateFlow (flujo de estados)
  → línea 33           = val uiState: StateFlow<...> = _uiState.asStateFlow()
  → línea 36           = init { loadData() } → ciclo de vida del ViewModel
  → línea 39           = viewModelScope.launch { ... }
                         → Lanzar corutina / viewModelScope (tipo de Scope)
  → línea 40           = delay(300L) → Corutinas / concurrencia asincrónica
  → línea 42-49        = _uiState.value = HomeUiState(...)
                         → actualización de estado desde corutina

  Conexión con UI (HomeScreen.kt:37):
  → val uiState by viewModel.uiState.collectAsStateWithLifecycle()
    → observar StateFlow desde Compose

====================================================
SLIDE 9 — CHECKBOX, RADIOBUTTON, SLIDER (0.5 min)
====================================================
Fragmento: SettingsScreen.kt líneas ~100-160

Conceptos cubiertos:
  → Checkbox(checked, onCheckedChange) → componente Checkbox
  → RadioButton(selected, onClick)     → componente RadioButton
  → Slider(value, onValueChange, valueRange, steps) → componente Slider
  → todos usan remember + mutableStateOf → gestión de estado local

====================================================
SLIDE 10 — PREVIEW + RESUMEN (0.5 min)
====================================================
Fragmento: LoginScreen.kt líneas ~191-207 (los @Preview)

Conceptos cubiertos:
  → @Preview(showBackground, name, uiMode) → Preview en Android Studio
  → SuperAhorroTheme(darkTheme = true/false) → Dark Mode / Light Mode

Cierre: mostrar la app corriendo en el emulador
  - SplashScreen → Login → Home (con ViewModel cargando)
  - Settings (Checkbox, RadioButton, Slider)

====================================================
GUION DE TIEMPO
====================================================
  Slide 1: 1:00 min
  Slide 2: 1:00 min
  Slide 3: 1:00 min
  Slide 4: 1:00 min
  Slide 5: 1:30 min
  Slide 6: 1:30 min
  Slide 7: 1:00 min
  Slide 8: 1:30 min
  Slide 9: 0:30 min
  Slide 10: 0:30 min
  ─────────────────
  TOTAL:   10:30 min (demo en vivo puede reemplazar últimas slides)

====================================================
CAPTURAS DE PANTALLA RECOMENDADAS PARA LA DEMO
====================================================
Orden sugerido para mostrar en el emulador durante la presentación:

1. LOGIN SCREEN
   → Mostrar campos email + contraseña + botón "Ingresar"
   → Mencionar: validación de estado, Scaffold, animaciones implícitas de Compose

2. HOME SCREEN — estado de carga
   → Mostrar el CircularProgressIndicator mientras carga
   → Mencionar: StateFlow, collectAsStateWithLifecycle, coroutines + delay

3. HOME SCREEN — cargado
   → Mostrar card de total del mes + StatCards (compras / supermercados)
   → Mostrar sección "Últimas compras" con PurchaseCards
   → Abrir Drawer lateral para mostrar logo Klarity + menú

4. HISTORIAL (HistoryScreen)
   → Lista completa de compras mockeadas
   → Campo de búsqueda con filtro en tiempo real

5. DETALLE DE COMPRA (PurchaseDetailScreen)
   → Card de info (supermercado, fecha, total)
   → Card de ticket con botón "Adjuntar" (FilledTonalButton)
   → Lista de productos con íconos de editar/eliminar
   → FAB para agregar producto

6. FORMULARIO DE PRODUCTO (ProductFormScreen)
   → Campo código EAN + botón de escaneo de cámara
   → Campos nombre, descripción, precio × cantidad
   → Card de subtotal calculado en tiempo real

7. ESTADÍSTICAS (StatsScreen)
   → Gráfico de barras mockeado (si está implementado)
   → Alternativa: mostrar datos de HomeUiState en HomeScreen

8. SETTINGS (SettingsScreen)
   → Toggle con Switch (dark mode)
   → RadioButton (moneda)
   → Slider (días de historial)
   → Checkbox (notificaciones)
   → Mencionar: estos son los componentes Material3 pedidos en clase

9. DARK MODE
   → Cambiar el tema del sistema → mostrar que la app responde automáticamente
   → Demostrar que todos los colores cambian (MaterialTheme.colorScheme)

TIPS DE DEMO:
- Abrir el drawer primero para mostrar el logo Klarity
- Navegar con la bottom nav bar para mostrar la navegación entre tabs
- Al mostrar ProductFormScreen, tipear un precio y ver el subtotal actualizarse en vivo

====================================================
NOTAS PARA LA PRESENTACIÓN
====================================================
- En cada slide: señalar la LÍNEA EXACTA del archivo con una flecha
- No leer el código — explicar QUÉ hace cada línea en términos del concepto
- "Aquí en línea 37, usamos LaunchedEffect que es un Side Effect de Compose..."
- Si preguntan por Intents implícitos: mencionar que navController.navigate()
  es el equivalente moderno del Intent explícito, y que se pueden usar
  Intent implícitos para compartir (p.ej. Intent(Intent.ACTION_SEND))
- Si preguntan por ViewBinding: explicar que Compose reemplaza completamente
  el sistema View+XML, por lo que ViewBinding no aplica
- Mencionar que la app tiene i18n (ES+EN), Dark/Light mode, y datos mockeados

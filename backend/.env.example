# Copiá este archivo como .env y completá con tus valores de Railway
DATABASE_URL=postgresql://usuario:password@host:port/dbname
JWT_SECRET=cambia_esto_por_una_clave_segura_larga
PORT=3000
GEMINI_API_KEY=        # Chatbot IA (chat.ts) y comparación de precios (prices.ts)
OPENROUTER_API_KEY=    # Escaneo de tickets con LLaMA Vision (ticket.ts) — openrouter.ai
